{
    // "extends": "../../../../tsconfig.json", // Removed because the file does not exist

    "compilerOptions": {
        "paths": {
            "core/*": ["./dev/core/dist/*"],
            "@lts/core/*": ["./lts/core/dist/*"]
        }
    }
}
